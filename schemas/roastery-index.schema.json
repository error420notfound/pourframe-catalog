{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "PourFrame roastery index",
  "type": "object",
  "additionalProperties": false,
  "required": ["schemaVersion", "roastery", "coffees"],
  "properties": {
    "$schema": { "type": "string" },
    "schemaVersion": { "const": 1 },
    "roastery": {
      "type": "object",
      "additionalProperties": false,
      "required": ["id", "name"],
      "properties": {
        "id": { "type": "string", "pattern": "^[a-z0-9]+(?:-[a-z0-9]+)*$" },
        "name": { "type": "string", "minLength": 1, "maxLength": 120 },
        "country": { "type": "string", "maxLength": 80 },
        "city": { "type": "string", "maxLength": 80 },
        "website": { "type": "string", "format": "uri" },
        "logoUrl": { "type": "string", "format": "uri" },
        "instagram": { "type": "string", "pattern": "^@[A-Za-z0-9._]+$" },
        "description": { "type": "string", "maxLength": 500 },
        "active": { "type": "boolean" },
        "sortOrder": { "type": "integer", "minimum": 0 }
      }
    },
    "coffees": {
      "type": "array",
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": ["id", "name", "status", "path", "updatedAt"],
        "properties": {
          "id": { "type": "string", "pattern": "^[a-z0-9]+(?:-[a-z0-9]+)*$" },
          "name": { "type": "string", "minLength": 1, "maxLength": 120 },
          "status": { "enum": ["available", "seasonal", "sold-out", "archived", "unknown"] },
          "path": { "type": "string", "pattern": "^roasteries/[a-z0-9-]+/coffees/[a-z0-9-]+\\.json$" },
          "featured": { "type": "boolean" },
          "sortOrder": { "type": "integer", "minimum": 0 },
          "updatedAt": { "type": "string", "format": "date" }
        }
      }
    }
  }
}
