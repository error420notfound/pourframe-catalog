{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "PourFrame roasteries index",
  "type": "object",
  "additionalProperties": false,
  "required": ["schemaVersion", "roasteries"],
  "properties": {
    "$schema": { "type": "string" },
    "schemaVersion": { "const": 1 },
    "roasteries": {
      "type": "array",
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": ["id", "name", "path"],
        "properties": {
          "id": { "type": "string", "pattern": "^[a-z0-9]+(?:-[a-z0-9]+)*$" },
          "name": { "type": "string", "minLength": 1, "maxLength": 120 },
          "path": { "type": "string", "pattern": "^roasteries/[a-z0-9-]+/index\\.json$" }
        }
      }
    }
  }
}
